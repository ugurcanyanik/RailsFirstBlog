<div class="comment clearfix">
	<div class="comment_content">
		<p class="comment_name"><strong><%= comment.name %></strong></p>
		<p class="comment_body"><%= comment.body %></p>
		<p class="comment_time"><h5><%= time_ago_in_words(comment.created_at) %> once yazildi</h5></p>
	</div>
  <p>
    <%= link_to 'sil', [comment.post, comment], method: :delete, class: "button", data: {confirm: 'emin misin?'} %>
  </p>
</div>
  <hr/>
